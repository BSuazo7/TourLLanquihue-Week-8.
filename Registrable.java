package model;

/**
 * Interfaz que define un contrato común para todas las entidades
 * que pueden ser registradas en el sistema de Llanquihue Tour.
 */
public interface Registrable {
    // Método abstracto que debe ser implementado por las clases
    void mostrarResumen();
}
